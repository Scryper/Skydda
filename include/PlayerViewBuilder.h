#ifndef PLAYERVIEWBUILDER_H
#define PLAYERVIEWBUILDER_H


class PlayerViewBuilder
{
    public:
        PlayerViewBuilder();
        virtual ~PlayerViewBuilder();
        PlayerViewBuilder(const PlayerViewBuilder& other);
        PlayerViewBuilder& operator=(const PlayerViewBuilder& other);

    protected:

    private:
};

#endif // PLAYERVIEWBUILDER_H
